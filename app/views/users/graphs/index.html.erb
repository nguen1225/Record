<table class="table table-hover">
  <thead>
    <tr class="tr_option">
      <th>カテゴリー</th>
    </tr>
  </thead>

  <tbody>
    <% @genres_graph_list.each do |genre| %>
    <tr>
      <td>
          <%= link_to "#{genre.name}", graphs_path(genre_id: genre.id) %>
        <% end %>
      </td>
      </tr>
  </tbody>
</table>

<%= paginate @genres_graph_list %>

  <br>
      <% if params[:genre_id].blank? %>  <!--all eventの時はグラフを出さない-->
      <% else %>
        <h2><%= @name %> value graph</h2>
        <hr>
          <%= column_chart @event_months %><br>
          <%= column_chart @event_now_month %>
          <%= column_chart @event_2month_ago %>
          <%= column_chart @event_3month_ago %>
      <% end %>




   <%# @event_months.each_with_index do |event_month, i| %>
   <%#= column_chart event_month.pluck(:title, :value), id: "total-attendance-chart-#{i}", width: '370px', height: '500px', colors: ['#C0C0C0'] %>