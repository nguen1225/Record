<p id="notice"><%= notice %></p>
<div class="title">
  <p><%= @name %></p>
</div>

<hr>

  <table class="table table-bordered">
    <thead>
      <tr class="tr_option">
        <th colspan="1"></th>
        <th>数値</th>
        <th>開始日</th>
        <th>終了日</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody id="event">
      <% @events_calender.each do |event| %>
          <%= render 'eventdestroy', event: event %>
      <% end %>
    </tbody>
  </table>
  <%= paginate @events_calender %>