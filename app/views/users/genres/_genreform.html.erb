<%= form_with(model: genre, url:genres_path, remote: true, method: :post) do |form| %>

<% if genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genre.errors.count, "error") %> prohibited this genre from being saved:</h2>

      <ul>
      <% genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <br>

  <div class="field">
    <%= form.label :name, "カテゴリー名" %>
    <%= form.text_area :name, size: "1x1", class: "form-control", required:true %>
  </div>
  <div class="actions">
    <%= form.submit('Create',class: "btn") %>
  </div>
<% end %>